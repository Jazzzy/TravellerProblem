//
// Created by Rubén Osorio López on 25/11/16.
//

#include "ProblemHelper.h"
//#include "LowerTriangularMatrix.h"

//extern LowerTriangularMatrix<int> *globDistanceMatrix;

//int ProblemManager::calculateCostFor(Solution *solution) {
//
//    int accumCost = 0;
//
//    accumCost += distanceMatrix->getElementUnsafe(0, (unsigned int) solution->getElemAt(0));
//    for (int i = 0; i < (sizeOfProblem - 2); i++) {
//        accumCost += distanceMatrix->getElementUnsafe((unsigned int) solution->getElemAt(i),
//                                                            (unsigned int) solution->getElemAt(i + 1));
//    }
//    accumCost += distanceMatrix->getElementUnsafe((unsigned int) solution->getElemAt(sizeOfProblem - 2), 0);
//    return accumCost;

//}